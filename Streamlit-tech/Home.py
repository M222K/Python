import streamlit as st 

st.write("Home Page")

#In order to have multiple pages -->
# '''
# create a multipage directory as pages and include all pages of website in order_page names
# '''

#now we will automatically have all the pages in the homepage, but as we switch between pages the data disappers, thus we need data to be stored in session state